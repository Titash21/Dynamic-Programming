import java.io.*;
public class rod_cutting{
  protected int calculateProfit(int[] price, int[] pieces,int length){

  }
  public static void main(String args[]){
    Scanner sc=new Scanner(System.in);
    int[] array=new int[5];
    int size=array.length;

    System.out.println("Enter the number of pieces"+size);
    System.out.println("The max profit to cut rod of length "+length+" is "+12);
  }
}
